# Python env for acquire / QA / harmonize / specificity / roll-up / verdict.
# Pinned for reproducibility (plan:0003 KD10); the conda-lock is the determinism
# precondition for byte-identical verdict.json.
name: ctpo-py
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.12
  - pandas=2.2.3
  - numpy=2.1.3
  - scipy=1.14.1          # spearmanr (concordance), gaussian_kde (near-zero antimode)
  - pyarrow=18.1.0
  - pyyaml=6.0.2
  - requests=2.32.3       # checksum-verified downloads
  - frictionless=5.18.0   # minimal datapackage.json (G1)
