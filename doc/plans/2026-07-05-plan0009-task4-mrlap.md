# plan:0009 Task 4 — Canonicalization adapter + MRlap overlap correction

> **For agentic workers:** implementation companion to `plan:0009` Task 4.
> Steps use `- [ ]` checkboxes. Recommended executor:
> `superpowers:subagent-driven-development` (this increment is larger than
> Tasks 2–3 — a new conda env, a new tool stack, and four new scripts — so
> split the dispatches along the Task boundaries below).

**Goal:** For each of the six eligible hormone strata, run the **MRlap
overlap-corrected** MR of germline hormone liability → long-COVID
(`GCST90454541`), fronted by a **hard-stop canonicalization adapter** that maps
each source family to MRlap-ready columns and injects the correct total `N`.
MRlap munges and runs cross-trait LDSC internally, returns the observed and
**overlap-corrected** causal effect, and we record the corrected effect beside
the Task-3 naive IVW — under the same **ancestry-flag / non-primary (KD1)** and
**bounded-sex (KD3)** ceilings, which overlap-correction does **not** lift.

**Design of record:** `entities/plans/0009-wave1-mr-hormone-pilot.md` (Task 4 +
Decision criteria). **Estimand:** `doc/plans/2026-07-03-wave1-gwas-mr-estimand.md`.
**Scale/overlap contract:** `plan:0008` KD2 + KD-scale; bounded-sex framing
`plan:0008` KD3. **Predecessor increments:**
`doc/plans/2026-07-04-plan0009-task2-hormone-instruments.md` (instruments) and
`doc/plans/2026-07-05-plan0009-task3-naive-mr.md` (naive comparator — the arm
this one is compared against).

**Architecture:** Extend the *isolated* `code/workflows/wave1-mr-hormone/`
Snakemake workflow — **not** the frozen `plan:0007` `wave1-mr/` run-of-record.
This is the one increment with a **genuinely new mechanism** (MRlap + its
internal GenomicSEM LDSC engine), so unlike Tasks 2–3 it does not reuse the
`plan:0007` estimator core. Four new scripts: (1) a **canonicalization adapter**
(`canonicalize_sumstats.py`) that reads each raw genome-wide harmonised sumstat,
maps its source-family columns to the MRlap canonical schema
(`rsid chr pos a1 a2 beta se N`), **injects the resolved total N**, and
**hard-stops** on any missing column or unresolved N; (2) a **setup rule**
(`setup_mrlap.R`) that installs MRlap + GenomicSEM at **pinned commits** into a
new `r-mrlap` conda env and hard-fails unless the resolved `GithubSHA1` matches;
(3) the **MRlap runner** (`run_mrlap.R`), one call per stratum × the shared
outcome, passing the raw canonical sumstats + the staged `eur_w_ld_chr` / HapMap3
references; (4) an **aggregator** (`aggregate_mrlap.py`) that joins the six MRlap
records with the Task-3 naive manifest into `mrlap_results.json`, placing the
overlap-corrected effect beside the naive IVW and stating the **instrument-set
non-identity** caveat.

**Tech stack:** Snakemake `--use-conda`; R 4.5 (`MRlap` + `GenomicSEM` — both
GitHub-only, no CRAN release — installed at pinned commits; `data.table`,
`lavaan`); Python 3.12 (`stdlib` `gzip`/`csv`; no pandas needed) for the
canonicalization adapter and aggregator (`stage.yaml` env). **No Python `ldsc`**
— MRlap's LDSC engine is GenomicSEM (pure R).

## Global constraints (from plan:0009 / plan:0008, binding)

- **Interpretation ceiling holds even after overlap correction.** Every estimate
  is **ancestry-flagged and non-primary** (KD1: outcome `GCST90454541` is the
  European-dominant ~85–90% multi-ancestry HGI broad/population meta; no EUR-only
  sibling) and any male-vs-female read is a **bounded exposure-architecture** probe
  (KD3), **not** a genotype×sex effect-modification test. **Correcting the UKB
  sample overlap does not lift either ceiling** — no MRlap result is reported as
  primary evidence for `hypothesis:0005` / `question:0007` / `question:0013`, nor
  as a sex-modification claim.
- **MRlap `N` is TOTAL, not effective.** For the binary outcome inject
  `N = cases + controls` (Σ per-ancestry `sample_size` from the `GCST90454541`
  `-meta.yaml` = **1,100,445**), **not** effective N; each Ruth stratum carries its
  continuous-trait sample size (pinned from the GWAS Catalog study record). The
  adapter **hard-stops** if N is unresolved.
- **Naive↔MRlap instrument-set non-identity.** MRlap selects instruments by its own
  genome-wide **distance pruning** (`MR_pruning_dist` / `MR_pruning_LD`), which is
  **not** the naive arm's 1000G-EUR LD-clump (r² < 0.001, 10 Mb). The naive↔corrected
  delta therefore conflates the overlap correction with an instrument-set difference —
  it is **not pure overlap bias**. This must be stated wherever the two are shown together.
- **Same-scale, different-estimator.** Both the naive IVW and the MRlap corrected
  effect are **log-OR long-COVID per 1-SD hormone** (same scale — MRlap operates on
  the raw betas), so they are scale-comparable; the caveat above is about the
  instrument set, not the scale. Record the scale label on every estimate; do not
  manufacture a scale mismatch, and do not present the delta as like-for-like overlap bias.
- **Two exposure-side caveats carried into interpretation.** SHBG and total
  testosterone are **not independent** (shared Ruth loci; steroid-axis pleiotropy);
  and the **female-testosterone stratum is weakest-instrumented yet most
  decision-relevant** — a wide/weak corrected estimate is informative, not a failure.
- **Weak is informative; technical faults hard-stop.** A wide or weak corrected
  estimate (esp. female testosterone) is a valid scientific outcome. MRlap failing to
  run, failing to reconcile scales/references, or returning a non-finite corrected
  effect from a well-instrumented stratum is a **technical fault** → hard-stop and repair.
- **Reproducibility.** MRlap **and** GenomicSEM commits pinned and asserted at setup;
  MR pruning/threshold params pinned in `config.yaml`; no Python-`ldsc` in the lock
  set. Payloads/outputs stay **off Dropbox** under `data/` + `results/` (gitignored,
  symlinked to `/data`); `git status` shows nothing under `data/`.

---

## Inputs (all present on disk from Tasks 1–3; no new dataset)

- **Exposures (raw genome-wide sumstats)** — the six Ruth harmonised
  `*.h.tsv.gz` at `data/raw/gwas/hormone-pilot/` (hm_-prefixed GWAS-Catalog SSF).
  MRlap consumes the **full genome-wide** sumstats (not the Task-2 clumped
  instruments) — it does its own instrument selection and LDSC munge.
- **Outcome** — `covid19-hgi-longcovid-broad-population.GCST90454541.h.tsv.gz`
  (non-hm GWAS-Catalog SSF; `beta` = log-OR; European-dominant — the ancestry flag).
- **Cross-trait LDSC reference** (staged Task 1, `dataset:eur-ldsc-ld-score-reference`):
  `data/raw/ldsc/eur_w_ld_chr/` (extracted) + `data/raw/ldsc/w_hm3.snplist`.
- **Task-3 naive manifest** — `results/wave1-mr-hormone-pilot/naive_mr_results.json`
  (the naive IVW/Egger/WM to compare against). If absent in a fresh worktree, the
  Snakemake DAG re-aggregates it cheaply (the per-stratum naive outputs persist on
  shared `/data`; no re-estimation of the outcome stream-extract is needed).
- **Setup sentinel (new)** — `.setup_mrlap.json` written by `setup_mrlap.R`.
- **Data-access gate** already passed at Task-1 staging; `consumed_by` on
  `ruth-2020-shbg-testosterone-gwas`, `covid19-hgi-longcovid-gwas`,
  `1000g-eur-ld-panel`, and `eur-ldsc-ld-score-reference` already carries
  `plan:0009` + `task:t089`. **This increment consumes only already-gated inputs —
  no entity edits, no new dataset.**

### Pinned per-stratum total N (GWAS Catalog study records; internally consistent)

| stratum | accession | N | check |
|---|---|---|---|
| ruth-shbg-combined | GCST90012111 | 370,125 | ≈ male+female (370,199) |
| ruth-shbg-male | GCST90012109 | 180,726 | |
| ruth-shbg-female | GCST90012107 | 189,473 | |
| ruth-testosterone-combined | GCST90012114 | 425,097 | ≈ male+female (424,907) |
| ruth-testosterone-male | GCST90012113 | 194,453 | |
| ruth-testosterone-female | GCST90012112 | 230,454 | |
| **outcome** GCST90454541 | — | **1,100,445** | Σ per-ancestry `sample_size` (case+control) |

### Pinned tool commits (both GitHub-only, no CRAN release)

| package | repo | commit | date |
|---|---|---|---|
| MRlap | `n-mounier/MRlap` | `660f026864f8bfbbad5a8206bdff7d58f5d5d05b` | 2025-01-09 (adds MR-Egger + pruned-SNP df) |
| GenomicSEM | `GenomicSEM/GenomicSEM` | `0a63ac0ea01b61d28bd17e4a204e0fa561ce5040` | 2026-06-02 |

## File structure

```
code/workflows/wave1-mr-hormone/
├── config.yaml                    MODIFY  + mrlap: block; + n: on each exposure; + outcome total_n; + mrlap_env: pins
├── Snakefile                      MODIFY  + setup_mrlap, canonicalize_{exposure,outcome}, run_mrlap (wildcard),
│                                            aggregate_mrlap rules + mrlap target
├── envs/
│   └── r-mrlap.yaml               CREATE  conda compiled deps (r-base 4.5, lavaan, data.table, remotes, ...);
│                                            MRlap + GenomicSEM installed from pinned commits by setup_mrlap
└── scripts/
    ├── setup_mrlap.R              CREATE  install MRlap + GenomicSEM @ pinned commits; assert GithubSHA1; write sentinel
    ├── canonicalize_sumstats.py   CREATE  source-family → canonical (rsid chr pos a1 a2 beta se N); inject N;
    │                                        HARD-STOP on missing column / unresolved N / empty output
    ├── run_mrlap.R                CREATE  MRlap(exposure, outcome, ld, hm3, pinned params) per stratum →
    │                                        per-stratum mrlap JSON (+ full return dump); estimator hard-stop
    └── aggregate_mrlap.py         CREATE  join 6 mrlap results + Task-3 naive manifest → mrlap_results.json
└── tests/
    └── test_canonicalize.sh       CREATE  exercise the adapter hard-stops on tiny synthetic fixtures (no conda)
```

`run_mrlap.R` is a **new** script (no `plan:0007` analogue — MRlap is the one new
mechanism), written into the isolated workflow; the frozen `wave1-mr/`
run-of-record is never edited.

## config.yaml additions

Add `n:` to **each** of the six `exposures:` entries (co-located with the stratum,
value from the pinned-N table above), e.g.:

```yaml
  - name: "ruth-shbg-combined"
    accession: "GCST90012111"
    trait: "shbg"
    sex: "combined"
    # ...existing keys unchanged...
    n: 370125          # GWAS Catalog GCST90012111 initialSampleSize (European UKB continuous trait)
```

(the remaining five: `ruth-shbg-male` 180726, `ruth-shbg-female` 189473,
`ruth-testosterone-combined` 425097, `ruth-testosterone-male` 194453,
`ruth-testosterone-female` 230454). Add `total_n:` under `outcome:`:

```yaml
outcome:
  name: "covid19-hgi-longcovid-broad-population"
  # ...existing keys unchanged...
  total_n: 1100445     # case+control TOTAL: Σ per-ancestry sample_size from the -meta.yaml
                       # (179+5725+3536+1090649+356). MRlap wants TOTAL N, not effective N.
```

Then append the MRlap blocks:

```yaml
# --------------------------------------------------------------- mrlap -------
# plan:0009 Task 4. MRlap overlap-corrected MR. MRlap munges each raw canonical
# sumstat and runs cross-trait LDSC internally using the `ld` (eur_w_ld_chr) +
# `hm3` (w_hm3.snplist) references staged in Task 1. The pruning params below are
# MRlap's documented defaults, PINNED for reproducibility — and they DIFFER from
# the naive arm's 1000G LD-clump (naive↔corrected delta is NOT pure overlap bias).
mrlap:
  MR_threshold: 5.0e-8      # genome-wide-significant instruments (matches naive p-threshold)
  MR_pruning_dist: 500      # kb — MRlap distance-based pruning window
  MR_pruning_LD: 0          # 0 = pure distance pruning (no LD-based pruning)
  MR_reverse: 1.0e-3        # reverse-causality (Steiger-like) exclusion threshold
  ld: "data/raw/ldsc/eur_w_ld_chr"     # cross-trait LDSC LD-score directory (Task 1)
  hm3: "data/raw/ldsc/w_hm3.snplist"   # HapMap3 SNP list (Task 1)

# ------------------------------------------------------------ env (mrlap) ----
# plan:0009 Task 4. MRlap + GenomicSEM are GitHub-only. setup_mrlap installs each
# at a PINNED COMMIT into the r-mrlap env and HARD-FAILS unless the installed
# packageDescription()$GithubSHA1 matches. No Python `ldsc` in the lock set.
mrlap_env:
  mrlap_repo: "n-mounier/MRlap"
  mrlap_ref: "660f026864f8bfbbad5a8206bdff7d58f5d5d05b"        # 2025-01-09
  genomicsem_repo: "GenomicSEM/GenomicSEM"
  genomicsem_ref: "0a63ac0ea01b61d28bd17e4a204e0fa561ce5040"  # 2026-06-02
```

## canonicalize_sumstats.py — the hard-stop adapter

`--config <cfg> --source-family <ruth-exposure|hgi-outcome> --stratum <name|"outcome">
--in <raw.h.tsv.gz> --out <canonical.tsv.gz>`. Streams the gz line-by-line
(stdlib `gzip` + `csv`; no full load), maps the source-family columns to the
canonical schema, injects the resolved total N, writes a lean 8-column gz
(`rsid chr pos a1 a2 beta se N`) — dropping the ~15 unused columns.

```python
RUTH_MAP = {   # hm_-prefixed GWAS-Catalog harmonised SSF (Ruth continuous traits)
  "rsid": "hm_rsid", "chr": "hm_chrom", "pos": "hm_pos",
  "a1": "hm_effect_allele", "a2": "hm_other_allele",   # a1 = effect allele (beta's ref)
  "beta": "hm_beta", "se": "standard_error"}           # SE is flip-invariant; non-hm col is correct
HGI_MAP = {    # non-hm GWAS-Catalog harmonised SSF (HGI long-COVID outcome)
  "rsid": "rsid", "chr": "chromosome", "pos": "base_pair_location",
  "a1": "effect_allele", "a2": "other_allele",
  "beta": "beta", "se": "standard_error"}
```

**Hard-stops (all loud, non-zero exit):**
1. **Missing column** — any mapped source column absent from the raw header →
   `stop`, naming the missing column and the source family.
2. **Unresolved N** — resolve N from the config (`exposures[stratum].n` for a Ruth
   exposure; `outcome.total_n` for the outcome). If absent, non-integer, or ≤ 0 →
   `stop`. N is written as a constant column on every row.
3. **Empty output** — if 0 data rows survive (after dropping rows with a missing
   `rsid`/`beta`/`se`, whose count is logged) → `stop` (a canonical file with no
   usable rows is a fault, not a quiet empty artifact).

The adapter does **not** do row-level QC beyond dropping unusable rows — MRlap
munges (strand/allele/NA handling) internally. It writes a small sidecar
`{out}.canonical.json`: `{source_family, stratum, n_injected, n_rows_in,
n_rows_out, n_dropped, columns_mapped}` for provenance.

## setup_mrlap.R — pinned-commit install + assert (mirrors setup_twosamplemr.R)

`--config <cfg> --sentinel <path>`. In the `r-mrlap` conda env:
`remotes::install_github(cfg$mrlap_env$genomicsem_repo, ref = <genomicsem_ref>,
upgrade = "never")` then the same for MRlap. Then **assert** each installed
package's recorded commit matches the pin:

```r
for (pkg in c("GenomicSEM", "MRlap")) {
  sha <- utils::packageDescription(pkg)$GithubSHA1
  want <- if (pkg == "MRlap") cfg$mrlap_env$mrlap_ref else cfg$mrlap_env$genomicsem_ref
  if (is.null(sha) || substr(sha, 1, 40) != substr(want, 1, 40))
    stop(sprintf("setup_mrlap: %s GithubSHA1 %s != pinned %s — HALT", pkg, sha, want))
}
```

Write the sentinel `.setup_mrlap.json`: `{mrlap_sha, genomicsem_sha, r_version,
sessionInfo_pkgs, installed_at_note}` (mirrors the `setup_twosamplemr` sentinel
shape). The `r-mrlap` env first-build (r-base 4.5 + lavaan + compiled deps + two
GitHub source installs) is ~15–20 min — expected, one-time.

## run_mrlap.R — the MRlap call + hard-stop (one per stratum)

`--config <cfg> --stratum <name> --exposure <canonical.tsv.gz>
--outcome <canonical.tsv.gz> --results-out <json> --dump-out <rds>`. Resolves
`trait`/`sex` from `config$exposures` by `--stratum` (the `build_instrument.R`
pattern). Reads both canonical sumstats with `data.table::fread`, then:

```r
res <- MRlap::MRlap(
  exposure = as.data.frame(exp_dt), outcome = as.data.frame(out_dt),
  exposure_name = spec$name, outcome_name = cfg$outcome$name,
  ld  = cfg$mrlap$ld, hm3 = cfg$mrlap$hm3,
  MR_threshold   = as.numeric(cfg$mrlap$MR_threshold),
  MR_pruning_dist = as.numeric(cfg$mrlap$MR_pruning_dist),
  MR_pruning_LD   = as.numeric(cfg$mrlap$MR_pruning_LD),
  MR_reverse      = as.numeric(cfg$mrlap$MR_reverse),
  save_logfiles = FALSE, verbose = TRUE)
saveRDS(res, dump_out)          # full return preserved (provenance; nothing silently dropped)
```

**Extract + record** (from `res$MRcorrection` and `res$LDSC` — confirm the exact
field names against `?MRlap` / the pinned source during implementation; MRlap's
returned names are the source of truth):

- `observed_effect` (± se, p) — MRlap's own uncorrected estimate (its distance-pruned set)
- `corrected_effect` (± se, p) — **the overlap-corrected estimate**; `or = exp(b)`
- `test_difference` / `p_difference` — corrected-vs-observed difference test
- `m_IVs` — instruments MRlap used after its pruning (and, if present in this
  pinned build, the **pruned-SNP rsID set** — this materialises the naive↔MRlap
  instrument-set non-identity)
- LDSC block: `h2_exp`, `h2_out` (± se), `int_exp`, `int_out` (single-trait
  intercepts), `gcov`, and **`gcov_int`** — the **cross-trait LDSC intercept**, the
  quantity that encodes the UKB sample overlap and drives the correction — plus `rg`.

**Hard-stop (technical fault, mirrors Task 3):** after MRlap returns, if the
load-bearing fields cannot be located, or `corrected_effect`/`observed_effect`/
their SEs/p are non-finite, or `m_IVs` is absent/0, `stop(...)` naming the stratum.
A wide/weak *finite* corrected estimate is **not** a fault — record it.

**Per-stratum results JSON:**

```json
{
  "stratum": "ruth-testosterone-female", "accession": "GCST90012112",
  "trait": "total-testosterone", "sex": "female",
  "exposure": "ruth-testosterone-female", "outcome": "covid19-hgi-longcovid-broad-population",
  "exposure_n": 230454, "outcome_total_n": 1100445,
  "status": "corrected",
  "scale": "log-OR long-COVID per 1-SD hormone",
  "mrlap_observed":  {"b": <num>, "se": <num>, "pval": <num>},
  "mrlap_corrected": {"b": <num>, "se": <num>, "pval": <num>, "or": <num>},
  "difference": {"test": <num>, "pval": <num>},
  "m_ivs": <int>, "pruned_snps": [<rsid...>|null],
  "ldsc": {"h2_exp": <num>, "h2_out": <num>, "int_exp": <num>, "int_out": <num>,
           "gcov": <num>, "gcov_int": <num>, "rg": <num>},
  "overlap_signal": {"cross_trait_ldsc_intercept": <num>,
                     "_note": "gcov_int encodes the UKB sample overlap MRlap corrects for; MRlap does not emit a literal overlap fraction — the intercept + N are the correction inputs."},
  "mrlap_params": {"MR_threshold": 5e-8, "MR_pruning_dist": 500, "MR_pruning_LD": 0, "MR_reverse": 1e-3},
  "resources": {"wall_clock_s": <num>, "peak_memory_mb": <num>},
  "labels": { "ancestry_flag": "...(KD1, non-primary — overlap-correction does NOT lift this)...",
              "bounded_sex": "...(KD3, not a sex-modification test)...",
              "exposure_side": "...SHBG<->testosterone coupling; female-T weakest yet most decision-relevant...",
              "sample_overlap_corrected": true, "naive_comparator_only": false,
              "overlap_correction_does_not_lift_ceiling": true }
}
```

## aggregate_mrlap.py → mrlap_results.json

`--config <cfg> --results <6 jsons> --naive <naive_mr_results.json>
--benchmarks <6 tsv> --setup <.setup_mrlap.json> --manifest <out>`. Joins the six
MRlap records with the Task-3 naive manifest (aligning by `stratum`), **hard-stops**
on any missing/malformed input **and** on any `status: "corrected"` record with a
non-finite `corrected_effect`/`se`/`pval`/`or` (defense-in-depth re-assert of the
runner's gate). Emits under `results/wave1-mr-hormone-pilot/`:

```json
{
  "plan": "plan:0009-wave1-mr-hormone-pilot", "task": "t089",
  "stage": "Task 4 — MRlap overlap correction (canonicalized sumstats)",
  "labels": { "ancestry_flag": "...(KD1)...", "bounded_sex": "...(KD3)...",
              "exposure_side": "...", "sample_overlap_corrected": true,
              "overlap_correction_does_not_lift_ceiling": true },
  "mrlap_params": { "MR_threshold": 5e-8, "MR_pruning_dist": 500, "MR_pruning_LD": 0,
                    "MR_reverse": 1e-3, "ld": "...eur_w_ld_chr", "hm3": "...w_hm3.snplist" },
  "setup": { "mrlap_sha": "660f026...", "genomicsem_sha": "0a63ac0...", "r_version": "..." },
  "comparison": [
    { "stratum": "...", "trait": "...", "sex": "...",
      "naive_ivw":       {"b": <num>, "se": <num>, "pval": <num>, "or": <num>, "nsnp": <int>},
      "mrlap_observed":  {"b": <num>, "se": <num>, "pval": <num>},
      "mrlap_corrected": {"b": <num>, "se": <num>, "pval": <num>, "or": <num>},
      "delta_naive_vs_corrected": <num>,
      "cross_trait_ldsc_intercept": <num>, "m_ivs": <int>,
      "scale": "log-OR long-COVID per 1-SD hormone" }
  ],
  "comparison_note": "naive IVW is on the 1000G-EUR LD-clumped set (r2<0.001,10Mb); MRlap corrected is on MRlap's genome-wide distance-pruned set (MR_pruning_dist=500kb, MR_pruning_LD=0). Same SCALE (log-OR/SD), DIFFERENT instrument set — the delta conflates overlap correction with instrument-set non-identity and is NOT pure overlap bias.",
  "cross_stratum": {
    "_note": "BOUNDED exposure-architecture descriptor (KD3) on the CORRECTED estimates — sign concordance of the male-only vs female-only corrected effect per trait against the common mixed-sex outcome. NOT a sex-modification test.",
    "shbg":         {"male_corrected_sign": <int>, "female_corrected_sign": <int>, "male_female_sign_concordant": <bool>},
    "testosterone": {"male_corrected_sign": <int>, "female_corrected_sign": <int>, "male_female_sign_concordant": <bool>}
  },
  "summary": { "n_strata": 6, "n_corrected": <int>, "n_failed": <int> },
  "entities": [ "dataset:ruth-2020-shbg-testosterone-gwas", "dataset:covid19-hgi-longcovid-gwas",
                "dataset:eur-ldsc-ld-score-reference", "dataset:1000g-eur-ld-panel" ],
  "resource": { "per_stratum": [ {"stratum": "...", "wall_clock_s": <num>, "max_rss_mb": <num>} ],
                "peak_rss_mb": <max>, "total_wall_clock_s": <sum>,
                "_note": "genome-wide MRlap internal munge + cross-trait LDSC — the new, previously-untested resource step." }
}
```

`cross_stratum` is descriptive sign-concordance on the corrected estimates only; it
estimates **no** interaction and makes **no** sex-modification claim. Per the
interpretation nuance from the Task-3 read, do **not** assert three-method
concordance here — MRlap yields a single corrected estimate per stratum; the
male/female concordance statement is IVW-vs-corrected sign only.

## Snakefile additions

```python
# --- resolved MRlap targets (plan:0009 Task 4) -------------------------------
MRLAP_DIR   = f"{PROC}/mrlap"
CANON_DIR   = f"{MRLAP_DIR}/canonical"
SETUP_MRLAP = f"{MRLAP_DIR}/.setup_mrlap.json"
MRLAP_MANIFEST = f"{RESULTS}/mrlap_results.json"
LDSC_LD_DIR = f"{RAW}/ldsc/eur_w_ld_chr"        # MRlap `ld` dir (staged Task 1)
LDSC_HM3    = f"{RAW}/ldsc/w_hm3.snplist"        # MRlap `hm3` (staged Task 1)


rule mrlap:                                 # Task-4 target
    input:
        MRLAP_MANIFEST,


rule setup_mrlap:                           # install MRlap + GenomicSEM @ pinned commits
    output:
        sentinel=SETUP_MRLAP,
    conda:
        "envs/r-mrlap.yaml"
    params:
        config=CONFIGFILE,
    shell:
        "Rscript {SCRIPTS}/setup_mrlap.R --config {params.config} --sentinel {output.sentinel}"


rule canonicalize_exposure:                 # fan-out over the six strata
    input:
        raw=lambda w: f"{GWAS_DIR}/{w.stratum}.{STRATA[w.stratum]['accession']}.h.tsv.gz",
    output:
        canon=f"{CANON_DIR}/{{stratum}}.exposure.tsv.gz",
        sidecar=f"{CANON_DIR}/{{stratum}}.exposure.tsv.gz.canonical.json",
    conda:
        "envs/stage.yaml"
    params:
        config=CONFIGFILE,
    shell:
        "python {SCRIPTS}/canonicalize_sumstats.py --config {params.config} "
        "--source-family ruth-exposure --stratum {wildcards.stratum} "
        "--in {input.raw} --out {output.canon}"


rule canonicalize_outcome:
    input:
        raw=OUTCOME_FILE,
    output:
        canon=f"{CANON_DIR}/outcome.tsv.gz",
        sidecar=f"{CANON_DIR}/outcome.tsv.gz.canonical.json",
    conda:
        "envs/stage.yaml"
    params:
        config=CONFIGFILE,
    shell:
        "python {SCRIPTS}/canonicalize_sumstats.py --config {params.config} "
        "--source-family hgi-outcome --stratum outcome "
        "--in {input.raw} --out {output.canon}"


rule run_mrlap:                             # fan-out over the six strata
    input:
        exposure=f"{CANON_DIR}/{{stratum}}.exposure.tsv.gz",
        outcome=f"{CANON_DIR}/outcome.tsv.gz",
        ld=LDSC_LD_DIR,
        hm3=LDSC_HM3,
        setup=SETUP_MRLAP,
    output:
        results=f"{MRLAP_DIR}/{{stratum}}.mrlap.json",
        dump=f"{MRLAP_DIR}/{{stratum}}.mrlap.rds",
    benchmark:
        f"{MRLAP_DIR}/{{stratum}}.benchmark.tsv"    # genome-wide munge + LDSC: wall-clock + max_rss
    conda:
        "envs/r-mrlap.yaml"
    params:
        config=CONFIGFILE,
    shell:
        "Rscript {SCRIPTS}/run_mrlap.R --config {params.config} "
        "--stratum {wildcards.stratum} --exposure {input.exposure} "
        "--outcome {input.outcome} --results-out {output.results} --dump-out {output.dump}"


rule aggregate_mrlap:
    input:
        results=expand(f"{MRLAP_DIR}/{{stratum}}.mrlap.json", stratum=STRATA),
        benchmarks=expand(f"{MRLAP_DIR}/{{stratum}}.benchmark.tsv", stratum=STRATA),
        naive=NAIVE_MR_MANIFEST,
        setup=SETUP_MRLAP,
    output:
        manifest=MRLAP_MANIFEST,
    conda:
        "envs/stage.yaml"
    shell:
        "python {SCRIPTS}/aggregate_mrlap.py --config {CONFIGFILE} "
        "--results {input.results} --naive {input.naive} "
        "--benchmarks {input.benchmarks} --setup {input.setup} --manifest {output.manifest}"
```

`PROC`, `RESULTS`, `RAW`, `SCRIPTS`, `CONFIGFILE`, `GWAS_DIR`, `OUTCOME_FILE`,
`STRATA`, and `NAIVE_MR_MANIFEST` all already exist in the Snakefile.

## Tasks

- [ ] **Step 1 — config.** Add `n:` to each of the six `exposures:` entries
      (pinned-N table), `total_n:` under `outcome:`, and the `mrlap:` + `mrlap_env:`
      blocks (verbatim above).
- [ ] **Step 2 — env.** Create `envs/r-mrlap.yaml`: `conda-forge`/`bioconda`
      channels; `r-base=4.5`, `r-remotes`, `r-yaml`, `r-jsonlite`, `r-data.table`,
      `r-r.utils`, `r-lavaan`, `r-gdata`, `r-corpcor`, `r-matrix`, `gzip`, plus the
      compilers/`make` MRlap+GenomicSEM need to source-install. **No Python `ldsc`.**
- [ ] **Step 3 — setup script.** Write `scripts/setup_mrlap.R` (install both at
      pinned commits, assert `GithubSHA1`, write `.setup_mrlap.json`), mirroring
      `setup_twosamplemr.R`.
- [ ] **Step 4 — canonicalization adapter.** Write `scripts/canonicalize_sumstats.py`
      (source-family column map, N injection from config, the three hard-stops, lean
      8-column gz + sidecar).
- [ ] **Step 5 — adapter fixture test.** Write `tests/test_canonicalize.sh`
      exercising the hard-stops on tiny synthetic gz fixtures, no conda: (a) a
      Ruth-family fixture missing `hm_beta` → HARD-STOP "missing column"; (b) an
      outcome-family fixture with N absent from a stub config → HARD-STOP "unresolved
      N"; (c) a valid 3-row fixture → canonical 8-col output with N injected on every
      row and correct row count. Assert exit codes + output header/row count. Seconds; no MRlap.
- [ ] **Step 6 — MRlap runner.** Write `scripts/run_mrlap.R` (the `MRlap()` call
      with pinned params, full-return `saveRDS` dump, field extraction, the
      technical hard-stop, the per-stratum JSON). Confirm MRlap's exact return-field
      names against `?MRlap` / the pinned source while implementing.
- [ ] **Step 7 — aggregator.** Write `scripts/aggregate_mrlap.py` (join six MRlap
      results + the Task-3 naive manifest → `mrlap_results.json`; the `comparison`
      table, `comparison_note`, `cross_stratum` corrected sign-concordance,
      `resource`, `entities`; hard-stop on missing/malformed input and non-finite
      corrected estimate).
- [ ] **Step 8 — Snakefile.** Add `MRLAP_DIR`/`CANON_DIR`/`SETUP_MRLAP`/
      `MRLAP_MANIFEST`/`LDSC_LD_DIR`/`LDSC_HM3`, the `setup_mrlap`,
      `canonicalize_exposure`, `canonicalize_outcome`, `run_mrlap` (wildcard +
      `benchmark:`), and `aggregate_mrlap` rules, and the `mrlap` target.
- [ ] **Step 9 — setup env (real).** `uv run snakemake -s
      code/workflows/wave1-mr-hormone/Snakefile --use-conda -c1 setup_mrlap`
      (~15–20 min first build; assert PASS on both pinned SHAs). *(Worktree note:
      drive via the main checkout's `.venv/bin/snakemake` with cwd in the worktree —
      the editable `science` path-dep breaks `uv run` inside a worktree.)*
- [ ] **Step 10 — run (real data).** `uv run snakemake -s
      code/workflows/wave1-mr-hormone/Snakefile --use-conda -c1 mrlap`. Runs the six
      canonicalizations, six genome-wide MRlap fits (internal munge + cross-trait
      LDSC — the resource-heavy new step; `benchmark:` captures `max_rss` +
      wall-clock), and the aggregate. The naive manifest re-aggregates cheaply if a
      fresh worktree lacks it (per-stratum naive outputs persist on shared `/data`).
- [ ] **Step 11 — inspect.** Read `mrlap_results.json`: six strata each with naive
      IVW beside MRlap observed + **corrected** (b/se/pval/or), the cross-trait LDSC
      intercept, `m_ivs`, the `delta` and `comparison_note`, the corrected
      `cross_stratum` sign block, every label (incl. `sample_overlap_corrected` +
      `overlap_correction_does_not_lift_ceiling`), and `peak_rss_mb`. Confirm every
      corrected estimate is finite and sane, the LDSC h2/rg/intercepts are plausible,
      and the female-testosterone corrected estimate is recorded (wide/weak =
      informative, not an error).
- [ ] **Step 12 — validate + note.** `uv run --frozen science validate`; record a
      t089 note summarising per-stratum naive-vs-corrected IVW OR, the overlap
      intercept, corrected sign-concordance, and peak RSS; commit.

## Final validation / Definition of done

- All six strata produce a `{stratum}.mrlap.json` + `{stratum}.benchmark.tsv` +
  `{stratum}.mrlap.rds`, and appear in `mrlap_results.json`; the aggregator
  hard-stops on any missing/malformed input.
- **Canonicalization adapter ran with a non-silent hard-stop** on missing columns /
  unresolved N, exercised by the Step-5 fixture test (not merely spot-checked); the
  canonical schema is `rsid chr pos a1 a2 beta se N`; **MRlap `N` is TOTAL**
  (outcome case+control = 1,100,445; each Ruth stratum's continuous-trait N).
- **MRlap ran** (internal munge + cross-trait LDSC) for each stratum and returned a
  finite **corrected** estimate (± se, p), the observed estimate, `m_IVs`, and the
  cross-trait LDSC intercept — **enforced by a hard-stop** in `run_mrlap.R` and
  re-asserted in the aggregator.
- **Naive↔corrected placed side by side** with the **instrument-set non-identity**
  stated (`comparison_note`); no cross-scale claim (both are log-OR/SD — stated), and
  the delta is **not** presented as pure overlap bias.
- **Reproducibility:** MRlap **and** GenomicSEM `GithubSHA1` pinned and asserted at
  setup (`.setup_mrlap.json`); MR pruning/threshold params pinned in `config.yaml`;
  **no Python `ldsc`** in the lock set.
- **Scale/resource on real data:** per-stratum `max_rss_mb` + wall-clock from
  `benchmark:` for the genome-wide MRlap munge+LDSC in the manifest `resource` block;
  `peak_rss_mb` reported. (This is the new, previously-untested resource step.)
- **Every output carries the ancestry-flag + non-primary + bounded-sex labels plus
  `sample_overlap_corrected` and `overlap_correction_does_not_lift_ceiling`** — the
  KD1/KD3 ceilings still bind after overlap correction; the `cross_stratum` block is a
  descriptive corrected-sign read, not a sex-modification test.
- **Manifest input-traceability:** `mrlap_results.json` carries an `entities`
  cross-ref to the four consumed datasets (full `datapackage.json` bundle is Task 5).
- `git status` shows nothing under `data/`; `mrlap_results.json` lives under
  `results/wave1-mr-hormone-pilot/` (gitignored, regenerable); config pins reproduce it.

## Decision criteria (inherited from plan:0009)

- Mechanics-GO for the corrected arm: MRlap runs (internal munge + cross-trait
  LDSC), records the cross-trait LDSC intercept (overlap signal), and returns a
  finite corrected estimate in a clearly labelled scale beside the naive IVW, with
  the naive↔MRlap instrument-set non-identity stated; the genome-wide resource cost
  is recorded.
- A weak/wide corrected estimate (esp. female testosterone) is an **informative**
  outcome under the KD1/KD3 ceilings, **not** a pipeline failure.
- NO-GO / fix-first only on a **technical** fault: canonicalization hard-stop
  (missing column / unresolved N), MRlap cannot run or reconcile the LD-score/HapMap3
  references, or a non-finite corrected estimate from a well-instrumented stratum —
  repair before any corrected result is quoted even as exploratory.

## Out of scope (this increment)

- **The results write-up + go/no-go note** and the full reproducible bundle
  (`datapackage.json` / `qa_report.{json,md}` / `run_metadata.json` / env locks)
  (Task 5). This increment stops at `mrlap_results.json`.
- **Any reportable-primary or sex-modification claim** — barred by the KD1/KD3
  ceilings, which overlap-correction does **not** lift.
- **Arm A (SLE) sensitivity matrix, bidirectional MR, pre-registration, and the
  acceptance-gate-for-primary** — banked with `plan:0008`.
- **BMI-adjusted / bioavailable-testosterone / estradiol strata** — later increment.

## Notes on scope

One science-task increment sized like Tasks 2–3, but carrying the plan's **one
genuinely new mechanism** (MRlap + its GenomicSEM LDSC engine, on a new pinned
conda env). It stops at the overlap-corrected estimates placed beside the naive
arm — so the naive↔corrected comparison the design turns on **exists in data**
before Task 5 writes it up. The canonicalization adapter is deliberately a
separate hard-stop boundary (not folded into `run_mrlap.R`) so a schema/N fault
fails loud on a clean, materialised canonical artifact rather than deep inside an
MRlap call.
